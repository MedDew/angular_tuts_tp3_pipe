import { HorsepowerPipe } from './horsepower.pipe';

describe('HorsepowerPipe', () => {
  it('create an instance', () => {
    const pipe = new HorsepowerPipe();
    expect(pipe).toBeTruthy();
  });
});
